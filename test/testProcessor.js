const { Anomaly, Processor } = require("../index.js");
const assert = require("assert");


describe("Processor", function() {

    it("with { fingerprint : true }", function() {

        it("stores fingerprints after state change", async function() {
            
            
        });

    });

});